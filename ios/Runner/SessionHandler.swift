import Foundation
import Security

/*
 * Desenvolvido por Viktor Casado
 * SIGEA – Plataforma Institucional
 * Padrão IFAL / MEC
 *
 * SessionHandler.swift - Gerenciador de Sessão (Keychain)
 */

class SessionHandler {
    func saveToken(_ token: String) {
        // Salvar no Keychain
    }

    func getToken() -> String? {
        return nil
    }
}
