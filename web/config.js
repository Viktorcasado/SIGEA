/**
 * Desenvolvido por Viktor Casado
 * SIGEA – Plataforma Institucional
 * Padrão IFAL / MEC
 * 
 * Configurações da aplicação Web
 */

const appConfig = {
    appName: 'SIGEA',
    version: '1.0.0',
    institution: 'IFAL',
    environment: 'production',
    apiEndpoint: 'https://api.sigea.ifal.edu.br',
    theme: {
        primaryColor: '#0175C2',
        secondaryColor: '#FFFFFF'
    }
};

console.log('SIGEA Web Config Loaded');
