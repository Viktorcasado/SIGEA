/*
 * Desenvolvido por Viktor Casado
 * SIGEA – Plataforma Institucional
 * Padrão IFAL / MEC
 *
 * supabase_linux.cc
 */

#include <iostream>

class SupabaseLinux {
public:
    void Init() {
        std::cout << "Inicializando driver Supabase..." << std::endl;
    }
};
